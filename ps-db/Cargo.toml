[package]
name = "ps-db"
version = "0.1.0"
edition = "2021"

[dependencies]
ps-core = { path = "../ps-core" }
kiddo = { workspace = true, optional = true }
memmap2 = { workspace = true }
half = { workspace = true }
byteorder = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
zip = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
tempfile = { workspace = true }

[features]
default = []
kd-tree = ["kiddo"]
# Reserved for future parallel kd-tree queries. Default OFF; OFF on mobile.
# Marker only — no parallel code yet.
rayon = []
